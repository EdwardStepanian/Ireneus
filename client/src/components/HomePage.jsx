import React from 'react';
import {Card, CardTitle} from 'material-ui/Card';

const HomePage = ()=> (

	<Card className = "container">
		<CardTitle title = "Ireneus" subtitle="Blog" />
	</Card>

);

export default HomePage;